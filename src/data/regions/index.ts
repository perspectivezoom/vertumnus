import ny from '@/data/regions/ny';
import sfbay from '@/data/regions/sfbay';

/**
 * Explicit list of all regions. Values are unvalidated — parse with
 * RegionSchema before use. Add new regions here; nothing is auto-discovered.
 */
export const regions = [sfbay, ny];
